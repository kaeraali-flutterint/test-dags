[project]
name = "test-dags"
dynamic = ["version"]
dependencies = [
  "ruff==0.15.21",
  "apache-airflow==3.3.1",
  "apache-airflow[amazon]",
  "paramiko",
  "pytest",
]
[tool.setuptools.packages]
find = {}
[tool.ruff.lint]
select = ["AIR", "E4", "E7", "E9", "F"]
