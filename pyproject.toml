[project]
name = "test-dags"
dynamic = ["version"]
dependencies = [
  "ruff",
  "apache-airflow==3.2.1",
  "apache-airflow[amazon]",
  "paramiko",
  "pytest",
]
[tool.setuptools.packages]
find = {}
[tool.ruff.lint]
select = ["AIR", "E4", "E7", "E9", "F"]
